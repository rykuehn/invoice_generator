import React from 'react';
import styled from 'styled-components';

const Title = styled.h1`
font-size: 1.5em;
color: blue;
`;

const Admin = () => {
  return <Title>Admin Landing Page</Title>;
};

export default Admin;
