import React from 'react';
import styled from 'styled-components';

const Title = styled.h1`
font-size: 1.5em;
color: green;
`;

const Client = () => {
  return <Title>Client Page</Title>;
};

export default Client;
